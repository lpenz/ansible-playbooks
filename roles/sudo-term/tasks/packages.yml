---
- name: install base packages
  apt:
    state: present
    package:
      - ansible
      - cryptmount
      - gawk # required by tmux-fingers
      - git
      - keychain
      - locales
      - mc
      - neovim
      - network-manager
      - openssh-server
      - pmount
      - renameutils
      - rkhunter
      - syncthing
      - tmux
      - ufw
      - vim
      - zsh
